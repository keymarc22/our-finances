<%= turbo_frame_tag 'modal' do %>
  <div class="mb-5 flex flex-col space-y-1.5 text-start sm:text-left">
    <h2 class="text-lg font-semibold leading-none tracking-tight">Edit bill</h2>
    <p class="text-xs text-muted-foreground">
      Update the details for this recurring payment.
    </p>
  </div>

  <%= simple_form_for @monthly_bill do |f| %>
    <%= render partial: 'form', locals: { f: f } %>

    <div class="flex justify-end gap-1">
      <%= render ButtonComponent.new(text: 'Cancel', theme: :secondary, "data-action": "click->modal#close") %>
      <%= render ButtonComponent.new(text: 'Save', theme: :primary, type: 'submit') %>
    </div>
  <% end %>
<% end %>
