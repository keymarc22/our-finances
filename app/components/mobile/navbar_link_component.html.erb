<%= link_to path, class: "col-span-11 flex flex-col items-center p-1 text-gray-500 hover:text-blue-500 transition-colors duration-200 #{active? ? 'text-sky-700 font-bold' : '' }" do %>
  <%= lucide_icon icon %>
<% end %>